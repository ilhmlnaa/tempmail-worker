name = "tempmail-worker"
compatibility_date = "2024-10-22"
main = "src/index.ts"

# ── Email Worker (catch-all for your domain) ──
# KV binding "MAIL_LITE" configured via Cloudflare Dashboard
# Uncomment after enabling Email Workers in CF dashboard
# [[email_rules]]
# name = "catch-all"
# matcher = { type = "all" }
# forward_to = "tempmail-worker"

[vars]
AUTH_SECRET = "change-me-to-random-string"
